# Dependencies
node_modules/

# Build and dist outputs
dist/
build/
hosting_updates/
.firebase/
RELEASE_NOTES_CURRENT.md

# Reverse engineering references and binary apps
reference/

# Local database & profiles storage (contains user session cookies & tokens)
local_db/
profiles/

# Self-hosted server: local Postgres backup dumps (contain every team account,
# profile, cookie, and audit log — never commit these)
/backups/
*.json
!package.json
!package-lock.json
!firebase.json

# Environment and logs
.env
*.log

# OS & IDE files
.DS_Store
.idea/
.vscode/
